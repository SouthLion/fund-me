// import ether.js
// create main function
// execute main function

const { ethers } = require("hardhat")

async function main() {
 

}


main().then().catch((error) => {
    console.error(error);
    // 正常退出0，非正常退出1
    process.exit(1);
})